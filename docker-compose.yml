services:
  bot:
    build: .
    env_file: .env
    environment:
      - DB_PATH=/app/data/steam_watcher.db
      - DOTABUFF_BROWSER_ENABLED=1
    volumes:
      - ./data:/app/data
      - ./.cache:/app/.cache
    restart: unless-stopped
